---
title: 'Configure Standards tiers and deployment options'
description: 'Define implementation levels, rank them and set targets for PSA company types.'
icon: 'layer-group'
---

**Audience:** MSP staff with Standards settings Manage permission and active billing. Company users can read shared linked work; they do not configure this catalog.

Open **Settings > MSP Tools > Standards**. Each Standards tier is a service plan, such as Platinum, Gold, Silver or Bronze. A company's plan decides which standards are expected of it. The names and order are your choices.

## Create and order plans

1. Open the **Standards tiers** tab and select **Add tier**. A numbered placeholder row is saved immediately.
2. In the row, enter a distinct **Name** and a **Description** of what the plan includes.
3. Choose its **Color**. Platinum, Gold, Silver and Bronze colors are available. Color identifies the plan visually; it does not determine its rank.
4. Use the row's up and down arrows to rank plans **highest first**.
5. Wait for automatic saving and reopen the page to confirm the configuration.

There is no overall Save button. Names and descriptions save shortly after typing stops; ordering is saved when you use an arrow.

## Assign company types to a plan

In a plan's row, open **Company types** and tick the PSA company types on that plan. Close the list to save. A company gets its plan from its company type. If a company has several types, it gets the highest-ranked plan.

Each company type belongs to one plan. A type already on another plan shows that plan in brackets, for example **Managed (now Gold)**. Ticking it moves it to this row. Untick a type to leave it without a plan.

The choices come from types on active PSA-synced companies. This screen does not create PSA company types or change a company's types.

## Put standards in plans

On the **Standards** tab, open a standard and use its **Service plans** field to choose the plans it belongs to. For example:

| Plan | Standards expected |
| --- | --- |
| Gold | A, B and C |
| Silver | A, B and Y |
| Bronze | A |

A standard with no plans selected is expected of every company. A company whose type has no plan is expected to have every standard.

A plan is an expectation, not a deployment update. It does not install software or mark a company Deployed.

## Add deployment options to a standard

1. Expand the standard's category and open its item row.
2. Under **Deployment options**, select **Add option**. Create at least one tier first if the button is disabled.
3. Rename the new option to the implementation, such as a product or managed service choice.
4. Choose its **Standards tier** and enter optional **Configuration detail** explaining the implementation.
5. Wait for saving, then review the resulting option before using it in deployment tracking.

An option belongs to that individual standard. Several options can use the same tier. A new option initially uses the first, strongest tier; check that choice rather than assuming it is the lowest level. Rename **New deployment option** before adding another option to the same standard.

Adding an option does not assign it to every company. Company deployment tracking needs the relevant selected option before its deployed state is classified. See [Read the Standards matrix](/user-guides/msp-tools/read-standards-matrix).

<Frame caption="Synthetic example: a configured option appears under its tier in the standard column menu. This menu applies across the loaded company scope.">
  <img src="/images/product/standards-deployment-options-menu.png" alt="Synthetic Standards option listed beneath its tier, followed by Deployed — option not specified and other statuses." width="610" height="636" />
</Frame>

Use a [status cell](/user-guides/msp-tools/update-standard-deployments) for one company. Review [bulk scope](/user-guides/msp-tools/bulk-update-standards) before choosing an option from the column menu shown above.

## Interpret deployed and target states

A deployed item with no selected option can remain **Unclassified**. An implementation at or above the company's target meets the tier target. A lower tier can still be Deployed while showing an upgrade opportunity; it is not automatically changed to Not Started. With several deployed alternatives in a one-of group, the strongest deployed tier supplies the group comparison.

## Change or remove definitions

Renaming a tier changes its shared label. Moving it changes its rank. Changing an option's tier changes how companies using that option are classified.

An option's trash action deletes it immediately, without a separate confirmation. Deleting a selected option removes that selection from affected company tracking, but does not by itself reset its deployment status. Review the resulting unclassified state.

A tier's trash action opens **Delete Standards tier?**. Confirm **Delete tier** only after reviewing the impact. A tier cannot be deleted while options still use it; move or delete those options first. Successful tier deletion clears company type assignments to that tier. There is no automatic replacement target.

## FAQs

<AccordionGroup>
  <Accordion title="Where do I configure Standards tiers?">
    Open Settings > MSP Tools > Standards and select the Standards tiers tab.
  </Accordion>
  <Accordion title="Are tier names fixed to Gold, Silver and Bronze?">
    No. Choose names and descriptions appropriate to your MSP.
  </Accordion>
  <Accordion title="Which tier is strongest?">
    The first tier is strongest. Use up and down arrows to rank them; color does not determine strength.
  </Accordion>
  <Accordion title="Is a newly added tier only an unsaved draft?">
    No. Add tier saves a numbered placeholder immediately; its inline fields save automatically afterward.
  </Accordion>
  <Accordion title="Where do Company type choices come from?">
    They come from types on active PSA-synced companies. This screen does not create or change PSA company types.
  </Accordion>
  <Accordion title="What if a company has several company types?">
    It gets the highest-ranked plan among its types.
  </Accordion>
  <Accordion title="How do I take a company type off a plan?">
    Untick it in that plan's Company types list and close the list to save.
  </Accordion>
  <Accordion title="Why is Add option disabled?">
    Create at least one Standards tier first.
  </Accordion>
  <Accordion title="What tier does a new option start with?">
    The first, strongest tier. Review and change that choice as needed.
  </Accordion>
  <Accordion title="Does creating an option assign it to companies?">
    No. An option is an available implementation choice for one standard; company tracking needs the relevant selection.
  </Accordion>
  <Accordion title="Can a deployed standard be below its target?">
    Yes. It remains deployed while its lower tier indicates an upgrade opportunity.
  </Accordion>
  <Accordion title="Why is a deployed item Unclassified?">
    Its tracking record has no selected deployment option supplying a tier.
  </Accordion>
  <Accordion title="Can I delete a tier used by options?">
    Move or delete those options first. The action rejects deletion while options still use that tier.
  </Accordion>
  <Accordion title="Does deleting an option require confirmation?">
    Its trash action deletes immediately. Affected company selections are cleared without resetting deployment status.
  </Accordion>
  <Accordion title="What happens to company types when a tier is deleted?">
    Types on that tier are left without a plan; another plan is not automatically selected.
  </Accordion>
</AccordionGroup>
